# RD Request R001

- session: 2026-05-30-session-7ff682
- linked-prd: .peaks/2026-05-30-session-7ff682/prd/requests/R001.md
- type: bugfix

## Red-line scope

### In-scope
- `packages/client/src/pages/Chat.tsx` — setTimeout cleanup fix
- `packages/client/src/pages/Dashboard.tsx` — error handling fix
- `packages/client/src/pages/Experts.tsx` — dialog close fix
- `packages/client/src/pages/Profile.tsx` — useEffect dependencies fix
- `packages/client/src/pages/Settings.tsx` — useEffect dependencies fix
- `packages/client/src/pages/Register.tsx` — captcha error flow fix
- `packages/client/src/pages/ForgotPassword.tsx` — captcha error flow + error state fix

### Out-of-scope
- Design/styling changes
- New features
- Backend API changes
- Other pages not listed above

## Implementation evidence

### Fix 1: Chat.tsx — CRITICAL: setTimeout cleanup
- Added `deleteTimeoutRef` to store timeout ID
- Added cleanup useEffect to clear timeout on unmount
- Updated `handleDeleteMessage` to clear existing timeout before setting new one
- Clears timeout when confirming deletion

### Fix 2: Dashboard.tsx — CRITICAL: error handling
- Added `toast` import from sonner
- Added catch block in `loadData` to handle errors
- Shows user-friendly error toast on failure

### Fix 3: Experts.tsx — HIGH: dialog close
- Added `setDeleteConfirmOpen(false)` in `confirmDelete` finally block
- Dialog now closes after successful or failed deletion

### Fix 4: Profile.tsx — HIGH: useEffect dependencies
- Moved `loadTeams` and `loadMembers` function definitions BEFORE useEffect hooks
- Added `loadTeams` and `loadMembers` to dependency arrays
- Fixed potential ReferenceError from const declarations not being hoisted

### Fix 5: Settings.tsx — HIGH: useEffect dependencies
- Added `loadConfig` and `refreshStatus` to dependency array
- Zustand store functions are stable references, safe to include

### Fix 6: Register.tsx — MEDIUM: captcha error flow
- Changed `loadCaptcha` to return `Promise<boolean>` indicating success
- Only opens captcha modal if `loadCaptcha` returns true
- Removed unreachable outer catch block

### Fix 7: ForgotPassword.tsx — MEDIUM: error state consistency
- Standardized `setLocalError('')` to `setLocalError(null)` across all occurrences
- Consistent "no error" representation throughout the file

## Status
- created: 2026-05-30T13:36:20.494Z
- last update: 2026-05-30T14:56:30.881Z
- state: qa-handoff

## Verification
- [x] All 212 unit tests pass
- [x] TypeScript type check passes (no errors)
- [x] Code review completed
- [x] Security review completed
